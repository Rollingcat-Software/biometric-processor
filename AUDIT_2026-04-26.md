# FIVUCSAS biometric-processor Production Audit — 2026-04-26

> READ-ONLY audit by background agent. Findings transcribed from agent output.

## Live runtime — ALL PASS

| Check | Result |
|---|---|
| Container `biometric-api` status | UP 23h (healthy), port 8001 |
| `torch.cuda.is_available()` | **FALSE** (CPU-only confirmed) |
| Memory usage | 1.214 GiB / 4 GiB (30%) |
| CPU usage | 3.46% nominal |
| MODEL_DEVICE | `cpu` |
| FACE_DETECTION_BACKEND | `opencv` |
| FACE_RECOGNITION_MODEL | `Facenet512` (CPU-runnable, soft-warned) |
| DEMOGRAPHICS_ROUTER_ENABLED | `false` (gate active) |
| ACTIVE_GESTURE_LIVENESS_ENABLED | `false` (phase-gated) |

## PR-by-PR verification — ALL PASS

| PR | Claim | Evidence |
|---|---|---|
| #36 + #51 | Anti-spoof core (rPPG, temporal, light, moiré, device-spoof, reaction-eval, face-bbox) | All files present in `app/infrastructure/ml/liveness/`; integrated in main router |
| #52 | Startup validator rejects GPU-needing ML on CPU | `@model_validator` HARD-fails for retinaface/yolo/ArcFace/VGG-Face/GhostFaceNet when `ALLOW_HEAVY_ML=false`; SOFT-warn for Facenet512 (production exception) |
| #53 | Demographics router gated behind flag | Routes not registered in `main.py` when flag off; integration test confirms 404; OpenAPI schema excludes route when disabled |
| #55 | Gesture Phase 1 — landmarks-only `ActiveGestureLivenessManager` | Server re-runs geometry on submitted landmarks (does NOT trust client verdict); DTW + finger-open distances + anti-spoof thresholds applied |
| #54 | Dependabot uuid removal + sentry/webpack-plugin | Commit `a29812d`; deps clean |

## Anti-spoof module inventory

Location: `app/infrastructure/ml/liveness/`

| File | Purpose |
|---|---|
| `rppg_analyzer.py` | Remote photoplethysmography (green-channel pulse, Butterworth bandpass 50-150 BPM) |
| `temporal_consistency_analyzer.py` | Multi-frame temporal coherence |
| `moire_pattern_analysis.py` | Gabor kernel bank (4 orientations) + CLAHE; screen detection |
| `enhanced_liveness_detector.py` (1244 LOC) | LBP texture, Haar cascades, eye aspect ratio, color/freq |
| `screen_replay_anti_spoof.py` | Reflection analysis |
| `optimized_texture_liveness.py` | Gabor + FFT (~50ms target on CPU) |
| `hybrid_liveness_detector.py` | Multi-modal fusion |
| `device_spoof_risk_evaluator.py` | Moire + reflection + flicker + flash response + hole cutout + focal blur |
| `background_active_reaction_evaluator.py` (~1000 LOC) | Tremor variance, brightness std, facial feature detection |
| `face_signal_metrics.py` | Bounding-box stability, skin tone, iris clarity |
| `light_challenge_service.py` | Challenge generation + response scoring |
| `live_session_baseline_calibrator.py` | Per-session baseline |

**Total anti-spoof code**: ~3,911 LOC.

## Dependencies & CVE posture

- 170 packages installed; pinned via `requirements.txt`
- Last Dependabot sweep: 2026-04-20 (60 alerts → 22 distinct fixes; aiohttp 3.13.4, PyJWT 2.12.0, werkzeug 3.1.6, urllib3 2.6.3)
- `pip-audit` not in container (skip)
- 3 TODOs about model SHA256 pinning (DeepFace 0.0.98 — trusted version, low priority)

## Code health

- 59 test files (unit + integration + demo)
- API routes: 64 across 25 modules
- TODO/FIXME/HACK count: 3 (all about model SHA256 pinning)
- Pytest binary not in container image (CI integration gap, not runtime)

## Security posture

| Area | Status |
|---|---|
| API key auth | ✅ `SimpleAPIKeyMiddleware` enforces `X-API-Key` |
| CORS | ✅ Hardcoded origins (no wildcard) |
| Rate limiting | ✅ InMemoryRateLimitStorage 60/min default; cost-based per-endpoint |
| Security headers | ✅ HSTS + X-Frame-Options SAMEORIGIN |
| PIL bomb cap | ✅ `Image.MAX_IMAGE_PIXELS = 50_000_000` |
| Embedding encryption | ⚠️ WIP (pgvector at-rest covered by DB layer; app-level AES not implemented) |

## Recommendations

| Priority | Item | Action |
|---|---|---|
| P1 | Gesture Phase 1 feature flag rollout | Decide: flip `ACTIVE_GESTURE_LIVENESS_ENABLED=true` in next release? Tests green |
| P1 | Embedding encryption design | AES layer for pgvector embeddings (was flagged WIP in 2026-04-20 audit) |
| P2 | Model SHA256 pinning | Resolve 3 TODOs in `deepface_extractor.py` |
| P2 | pytest in container | Add to Dockerfile for CI integration tests |

**Overall: ✅ ALL CLAIMS VERIFIED. No P0 issues.**
