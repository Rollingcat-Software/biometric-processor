apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: biometric-processor

commonLabels:
  app.kubernetes.io/part-of: biometric-processor
  app.kubernetes.io/managed-by: kustomize

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - serviceaccount.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml
