# Railway deployment - CPU-only (smaller image size)
# NOTE: deepface is installed separately with --no-deps in buildCommand
# to prevent tensorflow GPU version from being installed

# Core API
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pillow>=10.3.0
numpy>=1.26.0
opencv-python-headless>=4.8.0
scikit-image>=0.21.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.26.0
psutil>=5.9.0
structlog>=24.0.0
aiofiles>=24.1.0

# DeepFace dependencies (manually listed since we use --no-deps)
mediapipe>=0.10.0
mtcnn>=0.1.0
retina-face>=0.0.1
gdown>=4.0.0
tqdm>=4.0.0
pandas>=2.0.0
Flask>=1.1.2
keras>=2.2.0

# PostgreSQL with pgvector
asyncpg>=0.29.0
pgvector>=0.2.4

# Redis
redis>=5.0.0
