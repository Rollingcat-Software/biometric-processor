# Base requirements - CPU-only ML dependencies
# Note: opencv-python-headless and deepface are installed
# separately in Dockerfile to avoid dependency conflicts
# Updated: 2026-03-28 - Security vulnerability fixes (Dependabot)

# ===========================================
# Core API Framework
# ===========================================
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0
python-multipart>=0.0.18
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
httpx>=0.28.0
aiofiles>=24.1.0
structlog>=25.0.0

# ===========================================
# Image Processing
# ===========================================
pillow>=11.0.0
numpy>=1.26.0
scikit-image>=0.24.0

# ===========================================
# OCR - Document Text Extraction
# ===========================================
pytesseract>=0.3.10

# ===========================================
# Machine Learning - TensorFlow CPU Only
# TF 2.21 + tf-keras (Keras 2 compat) for DeepFace support
# TF_USE_LEGACY_KERAS=1 set in Dockerfile to route tf.keras -> tf-keras
# ===========================================
tensorflow-cpu==2.21.0
mediapipe>=0.10.30

# DeepFace dependencies (deepface itself installed via --no-deps)
# DeepFace 0.0.98+: built-in anti-spoofing, YOLOv11/v12, GhostFaceNet, CenterFace
tqdm>=4.67.0
gdown>=5.2.0
pandas>=2.2.0
flask>=3.1.0
flask-cors>=4.0.1
fire>=0.4.0
tf-keras>=2.21.0
mtcnn>=0.1.0
retina-face>=0.0.1

# ===========================================
# Database - PostgreSQL + pgvector
# ===========================================
asyncpg>=0.30.0
pgvector>=0.4.0
psycopg2-binary>=2.9.10
cloud-sql-python-connector[asyncpg]>=1.0.0

# ===========================================
# Caching - Redis
# ===========================================
redis>=5.2.0

# ===========================================
# Monitoring
# ===========================================
prometheus_client>=0.21.0

# ===========================================
# Anti-Spoofing - UniFace MiniFASNet (ONNX)
# ===========================================
uniface>=3.0.0

# ===========================================
# Voice Biometric - Speaker Embeddings
# DO NOT change resemblyzer version!
# ===========================================
resemblyzer>=0.1.3
pydub>=0.25.1
scipy>=1.14.0

# ===========================================
# Authentication - JWT
# ===========================================
PyJWT>=2.10.0

# ===========================================
# Utilities
# ===========================================
psutil>=6.0.0
gunicorn>=23.0.0
ultralytics>=8.3.0

# ===========================================
# Transitive security fixes (pin minimums)
# ===========================================
certifi>=2024.7.4
urllib3>=2.3.0
requests>=2.32.0
werkzeug>=3.1.0
jinja2>=3.1.5
cryptography>=44.0.0
