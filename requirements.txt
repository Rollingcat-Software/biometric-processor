# Base requirements - CPU-only ML dependencies
# Note: opencv-python-headless and deepface are installed
# separately in Dockerfile to avoid dependency conflicts
# Updated: 2026-04-20 - Dependabot sweep (60 alerts): aiohttp 3.13.4,
# PyJWT 2.12.0, werkzeug 3.1.6, urllib3 2.6.3, orjson 3.11.6, flask 3.1.3,
# plus pinned transitives (filelock, protobuf, tqdm)

# ===========================================
# Core API Framework
# ===========================================
fastapi>=0.115.12,<1.0.0
uvicorn[standard]>=0.34.2,<1.0.0
python-multipart>=0.0.20
pydantic>=2.11.3
pydantic-settings>=2.9.1
python-dotenv>=1.1.0
httpx>=0.28.1
aiofiles>=24.1.0
structlog>=25.4.0

# ===========================================
# Image Processing
# ===========================================
pillow>=11.2.1
numpy>=2.2.5
scikit-image>=0.25.2

# ===========================================
# OCR - Document Text Extraction
# ===========================================
pytesseract>=0.3.13

# ===========================================
# Machine Learning - TensorFlow CPU Only
# TF 2.21 + tf-keras (Keras 2 compat) for DeepFace support
# TF_USE_LEGACY_KERAS=1 set in Dockerfile to route tf.keras -> tf-keras
# ===========================================
tensorflow-cpu==2.21.0
mediapipe>=0.10.30

# DeepFace dependencies (deepface itself installed via --no-deps)
# DeepFace 0.0.98+: built-in anti-spoofing, YOLOv11/v12, GhostFaceNet, CenterFace
tqdm>=4.67.1
gdown>=5.2.0
pandas>=2.2.3
flask>=3.1.3
flask-cors>=5.0.1
fire>=0.4.0
tf-keras>=2.21.0
mtcnn>=0.1.0
retina-face>=0.0.1

# ===========================================
# Database - PostgreSQL + pgvector
# ===========================================
asyncpg>=0.30.0
pgvector>=0.4.0
psycopg2-binary>=2.9.10
cloud-sql-python-connector[asyncpg]>=1.0.0

# ===========================================
# Caching - Redis
# ===========================================
redis>=5.3.0

# ===========================================
# Monitoring
# ===========================================
prometheus_client>=0.21.1

# ===========================================
# Anti-Spoofing - UniFace MiniFASNet (ONNX)
# ===========================================
uniface>=3.0.0
# CPU-only ONNX Runtime — required for direct ONNX model loading (voice ONNX migration,
# future client-side model parity checks). Ultralytics bundles its own but this ensures
# onnxruntime is an explicit, versioned dependency of this service.
onnxruntime>=1.18.0

# ===========================================
# Voice Biometric - Speaker Embeddings
# Resemblyzer 0.1.4 uses librosa for mel spectrograms.
# librosa >= 0.10.0 introduced @stencil + @guvectorize in core/audio.py and
# util/utils.py which crash at import time on Python 3.12 with numba >= 0.59
# due to 'get_call_template' AttributeError in the stencil/guvectorize path.
# NUMBA_DISABLE_JIT=1 does NOT help — guvectorize compiles eagerly at import.
# Fix: pin librosa to 0.9.2 (last version before the numba stencil usage).
# ===========================================
resemblyzer>=0.1.3
librosa==0.9.2
pydub>=0.25.1
scipy>=1.15.2

# ===========================================
# Authentication - JWT
# ===========================================
PyJWT>=2.12.0

# ===========================================
# Utilities
# ===========================================
psutil>=6.1.1
gunicorn>=23.0.0
ultralytics>=8.3.63

# ===========================================
# Transitive security fixes (pin minimums)
# ===========================================
certifi>=2025.4.26
urllib3>=2.6.3
requests>=2.32.3
werkzeug>=3.1.6
jinja2>=3.1.6
cryptography>=45.0.3
setuptools>=80.9.0
idna>=3.10
tornado>=6.5.1
aiohttp>=3.13.4
starlette>=0.46.2
# Transitive pins (Dependabot 2026-04-20)
filelock>=3.20.3
protobuf>=5.29.6
orjson>=3.11.6
tqdm>=4.67.1
